{
  "name": "qcom-signal-selector",
  "version": "1.0.0",
  "private": true,
  "description": "Signal Selector - Connected Broadband Intelligence Platform",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "backend": "uvicorn app.main:app --reload"
  }
}
